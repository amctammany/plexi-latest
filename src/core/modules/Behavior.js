import Module from '../Module';
var BehaviorStamp = {
  props: {
  },
  methods: {
  },

};

class Behavior extends Module {
  constructor(config) {
    super(BehaviorStamp, config);
  }
}

module.exports = Behavior;
